# Bindu Vet Journey

## Run the website

1. Install Node.js LTS.
2. Extract this ZIP.
3. Open the folder in VS Code.
4. Open the terminal and run:

npm install
npm run dev

Then open the localhost link shown in the terminal.

This is the frontend preview version. After the design is approved, we can connect a CMS so Bindu can post activities without coding.
